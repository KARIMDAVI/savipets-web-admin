/**
 * Shared Types
 * 
 * Common type definitions used across features
 */

// Re-export common types from main types file
export type {
  Booking,
  BookingFilters,
  BookingStatus,
  User,
  ServiceType,
} from '@/types';

